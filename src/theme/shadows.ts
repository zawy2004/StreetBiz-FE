/** Warm Indochine level-1 resting card elevation with soft terracotta undertone. */
export const cardShadow = '0 1px 3px 0 rgba(28, 35, 46, 0.04), 0 6px 16px -2px rgba(184, 74, 57, 0.05)';

/** Warm Indochine level-2 card hover elevation. */
export const cardHoverShadow = '0 4px 6px -1px rgba(28, 35, 46, 0.06), 0 12px 24px -4px rgba(184, 74, 57, 0.1)';

/** Warm Indochine level-3 modal / sheet elevation. */
export const sheetShadow = '0 20px 25px -5px rgba(28, 35, 46, 0.12), 0 8px 10px -6px rgba(28, 35, 46, 0.08)';
